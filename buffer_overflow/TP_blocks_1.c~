
#include <stdio.h>
#include <string.h>

#define SIZE 172
#define BLOCK_SIZE 32

int main(void)
{
  int n = SIZE;
  char t[SIZE];
  int i;

  for(i = 0; i < SIZE; ++i){
    t[i] = i % 12345 * i % 5432;
  }
  
  
  
  char s[19] = "hello, how are you?";
  char s2[19];
  strcpy(s2, s);
  printf("%s\n", s);
  printf("%s\n", s2);
}
